<Controls:MetroWindow x:Class="GraphDownloader.UI.GroupInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="GroupInfo" Height="400" Width="300">
    <Grid>
        <DockPanel LastChildFill="True">
            <StackPanel x:Name="pnlFooter" Height="40" DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                <Button x:Name="btnOK" Height="30" Content="OK" Click="btnOK_Click" Margin="5" Width="280" />
            </StackPanel>
            <StackPanel x:Name="pnlInfo" Orientation="Vertical" Margin="5" Height="315" Width="285">
                <DataGrid x:Name="lstHosts" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Controls:MetroWindow>